{
  "name": "webcomic-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/popup.ts --bundle --minify --outfile=dist/popup.js --platform=browser --target=es2020 && cp popup.html dist/popup.html && cp -r icons dist/",
    "watch": "esbuild src/popup.ts --bundle --outfile=dist/popup.js --platform=browser --target=es2020 --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
